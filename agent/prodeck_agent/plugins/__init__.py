"""Plugins de ação que acompanham o agente (exemplos/built-ins).

São registrados como entry points ``prodeck.actions`` no pyproject do próprio
agente — exatamente o mecanismo que um pacote externo usaria. Servem de exemplo
vivo de como escrever um plugin (ver ``notify.py``).
"""
